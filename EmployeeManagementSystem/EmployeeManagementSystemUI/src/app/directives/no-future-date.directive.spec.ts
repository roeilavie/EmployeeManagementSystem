import { NoFutureDateDirective } from './no-future-date.directive';

describe('NoFutureDateDirective', () => {
  it('should create an instance', () => {
    const directive = new NoFutureDateDirective();
    expect(directive).toBeTruthy();
  });
});
